/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as web3 from '@solana/web3.js'
import * as beetSolana from '@metaplex-foundation/beet-solana'
import * as beet from '@metaplex-foundation/beet'

/**
 * Arguments used to create {@link PlatformSettings}
 * @category Accounts
 * @category generated
 */
export type PlatformSettingsArgs = {
  platformTreasury: web3.PublicKey
  platformSigner: web3.PublicKey
}

export const platformSettingsDiscriminator = [27, 9, 42, 110, 168, 20, 184, 121]
/**
 * Holds the data for the {@link PlatformSettings} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class PlatformSettings implements PlatformSettingsArgs {
  private constructor(
    readonly platformTreasury: web3.PublicKey,
    readonly platformSigner: web3.PublicKey
  ) {}

  /**
   * Creates a {@link PlatformSettings} instance from the provided args.
   */
  static fromArgs(args: PlatformSettingsArgs) {
    return new PlatformSettings(args.platformTreasury, args.platformSigner)
  }

  /**
   * Deserializes the {@link PlatformSettings} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(
    accountInfo: web3.AccountInfo<Buffer>,
    offset = 0
  ): [PlatformSettings, number] {
    return PlatformSettings.deserialize(accountInfo.data, offset)
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link PlatformSettings} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey
  ): Promise<PlatformSettings> {
    const accountInfo = await connection.getAccountInfo(address)
    if (accountInfo == null) {
      throw new Error(`Unable to find PlatformSettings account at ${address}`)
    }
    return PlatformSettings.fromAccountInfo(accountInfo, 0)[0]
  }

  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(
    programId: web3.PublicKey = new web3.PublicKey(
      'hausS13jsjafwWwGqZTUQRmWyvyxn9EQpqMwV1PBBmk'
    )
  ) {
    return beetSolana.GpaBuilder.fromStruct(programId, platformSettingsBeet)
  }

  /**
   * Deserializes the {@link PlatformSettings} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf: Buffer, offset = 0): [PlatformSettings, number] {
    return platformSettingsBeet.deserialize(buf, offset)
  }

  /**
   * Serializes the {@link PlatformSettings} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return platformSettingsBeet.serialize({
      accountDiscriminator: platformSettingsDiscriminator,
      ...this,
    })
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link PlatformSettings}
   */
  static get byteSize() {
    return platformSettingsBeet.byteSize
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link PlatformSettings} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    connection: web3.Connection,
    commitment?: web3.Commitment
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(
      PlatformSettings.byteSize,
      commitment
    )
  }

  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link PlatformSettings} data.
   */
  static hasCorrectByteSize(buf: Buffer, offset = 0) {
    return buf.byteLength - offset === PlatformSettings.byteSize
  }

  /**
   * Returns a readable version of {@link PlatformSettings} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      platformTreasury: this.platformTreasury.toBase58(),
      platformSigner: this.platformSigner.toBase58(),
    }
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const platformSettingsBeet = new beet.BeetStruct<
  PlatformSettings,
  PlatformSettingsArgs & {
    accountDiscriminator: number[] /* size: 8 */
  }
>(
  [
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['platformTreasury', beetSolana.publicKey],
    ['platformSigner', beetSolana.publicKey],
  ],
  PlatformSettings.fromArgs,
  'PlatformSettings'
)
